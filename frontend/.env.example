# Example environment variables for the frontend UI
# In production, these should be securely injected via your hosting provider (e.g. Vercel/Cloudflare)

# URL of the Backend API
VITE_API_URL=http://localhost:8000

# API Key corresponding to the backend's required X-API-Key header
VITE_API_KEY=sih-demo-key
